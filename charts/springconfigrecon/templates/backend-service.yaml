apiVersion: v1
kind: Service
metadata:
  name: {{ include "springconfigrecon.fullname" . }}-backend
  labels:
    {{- include "springconfigrecon.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http-backend
      protocol: TCP
      name: http
  selector:
    {{- include "springconfigrecon.selectorLabels" . | nindent 4 }}
